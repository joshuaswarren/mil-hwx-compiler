# H13 concat named ISA hole (2026-09-13)

Host-only. No ANE, no SSH, no `ane-linux-experiments` edit or execute. No jwm1.

Executing model: `openai-codex/gpt-5.6-sol` (assigned route; no fallback observed).

## Source

- Compiler base: `1f672539699ce32fa4da4a876404559aec88c36e` (`feature/h13-bool-transpose`)
- Branch: `feature/h13-concat`

## Hole

H13 has no concat encoder. The decoded corpus writes one output surface from
one input or a broadcast pair. Tile replicates one input. Pad is refused.
There is no concat or copy capture, so joining independent sources cannot be
emitted.

Encoder heads_layout stack: concat axis=1 of fp16 `[1,1,375,128]`. 2-way,
8-way, and the 8-way binary tree all hit the same named diagnostic.

Standalone 2-way concat first failed `H13 function inputs must all be used`
because `values = (a, b)` is a tuple and `operands` only records Value-kind
arguments. Liveness now walks tuple/list elements. Valid concat then fails:

```text
6:5: error [h13.unsupported-concat]: H13 has no concat encoder: every decoded H13 program writes one output surface from one input or a broadcast pair, and the binding ABI carries one contiguous slice per binding, so joining independent sources into one buffer needs a data-movement program the decoded corpus does not hold
```

No program is emitted.

## Tests

`tests/test_h13_layout_cli.py`:

- 2-way concat axis=1 of two `[1,1,375,128]` fp16
- 8-way concat of eight `[1,1,375,128]`
- 8-way binary tree of 2-way concats

All pin `h13.unsupported-concat` / `H13 has no concat encoder`.

```text
sha256sum build/mil-hwxc
8406755b5033e06cc542b1a41af028d4f6ad835f707b3880dc10e0a2818cfeb0

make test-h13
  H13_ENCODING_OK
  H13 MIL-to-ANEC/HWX CLI: PASS (device-free)
  h13 split cli: PASS
  h13 layout cli: PASS
  h13 composite cli: PASS
  h13 batched cli: PASS
  h13 registry cli: PASS
  h13 boolean cli: PASS
  h13 tile cli: PASS
```

## Hardware boundary

No SSH, hardware inference, ANE command, router change, or Mac execution.
No files under `ane-linux-experiments` were edited or executed.
