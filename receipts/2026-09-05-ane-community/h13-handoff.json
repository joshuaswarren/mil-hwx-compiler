{
  "schema": "h13-hardware-handoff.v1",
  "purpose": "Everything the base-M1 owner needs to run the compiler's H13 packages once the ANE-enabled boot and driver are confirmed. Artifact readiness is not permission to submit.",
  "source_revision": "6d39ef5+ on joshuaswarren/mil-hwx-compiler feat/h13-m1",
  "host_verification": [
    "receipts/2026-09-05-ane-community/h13-wide-linux.log",
    "receipts/2026-09-05-ane-community/h13-wide-macos.log",
    "receipts/2026-09-05-ane-community/h13-mlp-block-smoke.log"
  ],
  "package": {
    "schema": "mil-hwxc.h13-anec-package.v1",
    "files": "program-N.anec per operation plus manifest.json",
    "anec_layout": "0x1000-byte header, 0x274-byte task descriptor at 0x1000, constants at content offset 0x280, 0x4000-byte tiles",
    "manifest": "tensors{}, programs[], dispatchPlan[], intermediates[]; bindings carry logical shape, ANEC channel index, physical nchw layout, logicalBytes, allocationBytes, and optional slice {tensor, elementOffset, elementCount, physicalElements}; folded constants appear as constantInputs hex plus binding=constant; shape-only aliases carry aliasOf; chunked matmul results carry accumulation=chunked-fp16",
    "buffer_conversion": "python3 research/inspect_anec.py PACKAGE --pack-input NAME dense.fp16 --output PATH | --pack-constant NAME --output PATH | --unpack-output NAME PATH --output dense.fp16 (PATH is a directory of program-<i>.<binding>.buffer files when a tensor is sliced across programs; padded lanes are zero-filled and dropped)"
  },
  "supported_now": [
    "add, mul, maximum, minimum, sub, real_div, relu, clip on any positive static fp16 shape (64-lane programs, zero-padded tail); constants folded on the host, sub via negation, real_div via exact reciprocals",
    "matmul and linear with any K and any N: 512-wide reduction chunks summed with add (chunked-fp16), 512-output chunks, zero padding, multiple rows as one program per row",
    "DAGs with fan-out (residuals, mul(x,x)); reshape, squeeze, expand_dims as zero-program aliases",
    "example: a 768->1024->768 MLP block with relu and residual compiles to 92 programs (8 matvec, 84 elementwise)"
  ],
  "first_hardware_checks": [
    "add with a=index*0.125 patterns and b=index*0.25 patterns; expect exact fp16 sums (same oracle as tests/hardware/run_alu.mm)",
    "matvec K=256 with the dense test weights from tests/test_h13_cli.py; compare against a CPU fp32-accumulated fp16 reference",
    "a two-program chain add->mul; the intermediate buffer is the first program's output channel 4 and the second program's input channel 5"
  ],
  "prerequisites_from_peer": [
    "Verified ANE-enabled Linux boot with the device-tree node present",
    "Exact driver and libane identities, and confirmation that the loader accepts the 0x1000-byte ANEC header and the channel 4/5/6 binding contract",
    "Explicit hardware handoff for this session"
  ],
  "linux_run": {
    "command": "ANE_CHECKOUT=~/src/omarchy-ane bash tests/run_h13_linux_hardware.sh MIL MODEL_ROOT x=input.fp16",
    "prerequisites": [
      "~/src/omarchy-ane on branch omarchy (f8df7bc: ANEC payload at 0x1000), libane/libane.a and bindings/python/dylib/libane_python.so built",
      "ane.ko loaded with /dev/accel/accel0 accessible",
      "verified ANE-enabled boot per homelab-infra docs/jw-m1.md bring-up ladder"
    ],
    "first_run": "a 64-element add (tests/test_h13_cli.py source()) before any matmul; the runner compares every output with tools/h13_reference.py and fails closed on any manifest or size mismatch before submission"
  },
  "macos_run": {
    "command": "bash tests/run_h13_hardware.sh MIL MODEL_ROOT x=input.fp16 (or make test-h13-hardware ...)",
    "prerequisite": "an arm64 Mac whose /Library/Caches/com.apple.aned/<build>/InMemoryModelCache is writable by root; on macOS 26.6 (25G83) that requires SIP disabled from Recovery, an owner decision. HWX container fields derived from the H13 fixture are listed with their unknowns in research/h13-hwx-fields.md"
  }
}
