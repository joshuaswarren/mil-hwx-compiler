program(1.3)
[buildInfo = dict<string, string>({})]
{
  func main<ios18>(tensor<fp16, [1, 512, 1, 1]> a) {
    int32 axis = const()[name = string("axis"), val = int32(1)];
    tensor<fp16, [1, 512, 1, 1]> y = softmax(x = a, axis = axis)[name = string("probabilities")];
  } -> (y);
}
