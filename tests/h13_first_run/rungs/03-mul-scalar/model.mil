program(1.3)
[buildInfo = dict<string, string>({})]
{
  func main<ios18>(tensor<fp16, [1, 64, 1, 1]> a) {
    fp16 c = const()[name = string("c"), val = fp16(0.5)];
    tensor<fp16, [1, 64, 1, 1]> y = mul(x = a, y = c)[name = string("scaled")];
  } -> (y);
}
