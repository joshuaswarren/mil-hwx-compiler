program(1.3)
[buildInfo = dict<string, string>({})]
{
  func main<ios18>(tensor<fp16, [1, 1, 128, 128]> x) {
    fp16 scale = const()[name = string("scale"), val = fp16(0.08838834764831845)];
    tensor<fp16, [1, 1, 128, 128]> t = exp(x = x)[name = string("t")];
    tensor<fp16, [1, 1, 128, 128]> y = mul(x = t, y = scale)[name = string("y")];
  } -> (y);
}
